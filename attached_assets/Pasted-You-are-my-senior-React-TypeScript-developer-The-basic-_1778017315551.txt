You are my senior React + TypeScript developer. The basic "AGJ Cinema" app is built. Now I need you to add advanced UI/UX improvements and features. Do NOT break any existing functionality. Work in the order given, and after finishing, give me a detailed summary in Arabic of everything you changed.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PART 1: Advanced Search Experience
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. In `src/pages/Search.tsx`:
   - ADD a row of GENRE FILTER chips below the search bar: Action, Comedy, Horror, Sci-Fi, Romance, Animation, Documentary, Thriller.
   - These chips scroll horizontally on mobile with hidden scrollbar.
   - Clicking a chip highlights it with cyan background and filters results by that genre.
   - Use TMDB's genre IDs: Action=28, Comedy=35, Horror=27, Sci-Fi=878, Romance=10749, Animation=16, Documentary=99, Thriller=53.
   - When a genre is selected AND a search query exists, use `searchMovies` with both query and genre filter.
   - If only genre is selected (no query), fetch movies by genre using TMDB's discover endpoint instead.
   - Add a "Clear Filters" button that appears when any filter is active.
   - Search results should show a subtle skeleton loading animation (pulsing gray rectangles) instead of a spinner for a more polished feel.
   - Add infinite scrolling: when user scrolls near the bottom of results, auto-load the next page of TMDB results and append them.

2. In `src/api/tmdb.ts`:
   - ADD a new function `discoverByGenre(genreId: number, page: number = 1)` that fetches from the discover endpoint.
   - ADD a new function `searchMulti(query: string, page: number = 1)` that searches both movies and TV shows combined.

3. In `src/hooks/useTmdb.ts`:
   - ADD `useDiscoverByGenre(genreId)` and `useSearchMulti(query)` hooks with pagination support via `useInfiniteQuery`.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PART 2: TV Shows Support
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. In `src/pages/Home.tsx`:
   - ADD two new rows below the movie rows:
     - "Popular TV Shows" using a new hook `usePopularTV()`.
     - "Top Rated TV Shows" using a new hook `useTopRatedTV()`.
   - When a TV show card is clicked, navigate to `/tv/{id}`.

2. CREATE `src/pages/TVDetails.tsx`:
   - Copy the structure from MovieDetails.tsx but adapted for TV shows.
   - Use TMDB TV endpoints: getTVDetails(id), getSimilarTV(id), getVideosTV(id).
   - Display: number of seasons, number of episodes, episode runtime, networks, created by.
   - Show a season selector dropdown.
   - When a season is selected, display its episodes in a grid.
   - Each episode card shows: still image, episode number, title, overview, air date.
   - A "Watch Episode" button that constructs the URL: `${VITE_STREAM_BASE_URL.replace('/movie/', '/tv/')}${id}/${season_number}/${episode_number}?primaryColor=...&ds_lang=ar`.
   - MoviePlayer handles TV URLs already — no changes needed to the player.

3. In `src/api/tmdb.ts`:
   - ADD functions: getPopularTV, getTopRatedTV, getTVDetails(id), getSimilarTV(id), getVideosTV(id), getSeasonDetails(tvId, seasonNum).
   - ADD TypeScript types for TVShow, TVSeason, TVEpisode.

4. In `src/hooks/useTmdb.ts`:
   - ADD corresponding hooks for the new TV functions.

5. In `src/App.tsx`:
   - ADD route: `/tv/:id` → TVDetails (lazy loaded).

6. In `src/components/MovieCard.tsx`:
   - Update it to accept an optional `mediaType` prop ("movie" | "tv").
   - If mediaType is "tv", navigate to `/tv/{id}` instead of `/movie/{id}`.
   - Use the same poster display, just different routing logic.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PART 3: Arabic Subtitles & Player Improvements
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. In `src/pages/Home.tsx` and `src/pages/MovieDetails.tsx`:
   - Modify the stream URL construction to include: `&ds_lang=ar&sub_lang=ar`
   - This enables automatic Arabic subtitle search via VidAPI's OpenSubtitles integration.
   - Final URL format: `base_url/movie/{id}?primaryColor=...&lang=en&ds_lang=ar&sub_lang=ar`

2. In `src/pages/TVDetails.tsx`:
   - Same subtitle params in the Watch Episode URL.

3. In `src/components/MoviePlayer.tsx`:
   - ADD a small floating info badge at the top-left that briefly appears ("Press Esc to close") and fades out after 3 seconds.
   - When the iframe loads, show a subtle loading pulse on the overlay background until the iframe fires its 'load' event.
   - ADD a "Report Issue" link at the bottom-right corner of the overlay: small text "Report broken stream?" in white/40. Clicking it opens a mailto link with subject "AGJ Cinema - Stream Issue - {title}".
   - Save progress to localStorage more aggressively: save every time a 'playing' postMessage is received, not just on close.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PART 4: Navigation & UI Polish
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. In `src/components/Navbar.tsx`:
   - The "TV Shows" link should scroll to the TV shows section on the homepage, or navigate to a new `/tv-shows` page showing all TV content.
   - ADD a subtle animation to the logo on hover: slight scale (scale-105) and glow (text-shadow cyan).
   - When user scrolls down, the navbar should get a more solid background (`bg-[#0a0a0a]/95 backdrop-blur-md`) with a thin bottom border (`border-b border-gray-800`).

2. In `src/components/BottomNav.tsx`:
   - ADD a "TV" tab in the middle, with a Tv icon from lucide-react. Path: `/tv-shows`.
   - Reorder tabs: Home, Search, TV, My List, Profile (5 items now).
   - All icons should maintain the same cyan active / gray inactive styling.

3. CREATE `src/pages/TVShows.tsx`:
   - A dedicated page for TV shows only.
   - Hero-style banner at top with a featured TV show (random from popular).
   - Rows: Popular, Top Rated, Airing Today, On The Air.
   - Each row uses MovieRow component but passes `mediaType="tv"`.

4. In `src/App.tsx`:
   - ADD route: `/tv-shows` → TVShows (lazy loaded).

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PART 5: Final Quality Checklist
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

After completing everything, verify:
- [ ] Search page has genre filters and infinite scroll.
- [ ] Skeleton loading is used instead of plain spinners.
- [ ] TV shows appear on homepage and have their own detail page.
- [ ] Episodes can be selected and played.
- [ ] Arabic subtitles auto-load in player.
- [ ] "Report broken stream" link works in the player.
- [ ] BottomNav has 5 tabs and all work.
- [ ] `/tv-shows` page exists and shows content.
- [ ] No existing functionality is broken.
- [ ] No ads anywhere.
- [ ] App name "AGJ Cinema" is preserved.

Give me a detailed Arabic summary of all changes, new files created, and any issues to be aware of.